spring:
  profiles:
    active: prod
  resources:
    chain:
      strategy:
        fixed:
          enabled: true
          paths: /**
          version: v5

type: Framework
name: Spring Boot ${type}
my:
  messageValue: ${random.int(1,10)}

